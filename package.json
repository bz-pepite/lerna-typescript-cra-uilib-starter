{
  "name": "lerna-typescript-cra-uilib-starter",
  "version": "0.0.0",
  "description": "Starter for Monorepos: Lerna, TypeScript, CRA and Storybook",
  "main": "index.js",
  "author": "shnyder.com",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.18.3"
  },
  "scripts": {
    "story": "lerna run story --stream",
    "prestart": "lerna run libbuild --stream",
    "libwatchbuild": "lerna run libwatchbuild --stream",
    "start": "lerna run start --stream"
  }
}